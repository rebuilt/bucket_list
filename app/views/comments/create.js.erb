var comment = { 
  'body': '<%= @comment.body %>',
  'commenter': '<%= @comment.user.name %>',
  'when': '<%= time_ago_in_words(@comment.created_at) %>'
};
Comments.addComment(comment);

Comments.clearInputField();
